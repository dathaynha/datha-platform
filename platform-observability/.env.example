# platform-observability — copy to .env and fill in; never commit .env.

# postgres-exporter connection. Point at your local platform Postgres
# (host.docker.internal = the macOS host from inside colima).
POSTGRES_EXPORTER_DSN=postgresql://postgres:password@host.docker.internal:5432/postgres?sslmode=disable
